require 'test_helper'

class ReservationTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end

  # r = Reservation.first
  # touch r.filepath(false)
  # r.filepath(true) == r.partial_filepath(0)
  # touch r.partial_filepath(0)
  # r.filepath(true) == r.partial_filepath(1)
end
